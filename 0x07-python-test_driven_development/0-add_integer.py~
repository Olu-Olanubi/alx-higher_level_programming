#!/usr/bin/python3
def add_integer(a, b=98):
    """
    adds two integers
    """
    if type(a) not in [int, float] or type(b) not in [int, float]:
        raise TypeError
    else:
        return int(a) +int(b)
